source 'https://rubygems.org'
gem 'github-pages'

# Needed for --watch on Windows (see http://jekyllrb.com/docs/windows/)
require 'rbconfig'
gem 'wdm', '~> 0.1.0' if RbConfig::CONFIG['target_os'] =~ /mswin|mingw/i